substitutions:
  name: main-bathroom
  friendly_name: Main Bathroom
  friendly_name_short: Main Bath
  board: esp01_1m
  event: nodered_automation

esphome:
  name: ${name}
  name_add_mac_suffix: false
  friendly_name: ${friendly_name}
  project:
    name: Generic.KS-602
    version: "0.9"
  
esp8266:
  board: ${board}
  restore_from_flash: true

preferences:
  flash_write_interval: 30s

# Enable logging
logger:

# Enable Home Assistant API
api:

ota:
  - platform: esphome

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "KS-602 Fallback Hotspot"

#captive_portal:
#switch:

# See: https://esphome.io/guides/automations.html#global-variables
globals:
  - id: glbl_detach_relay
    type: bool
    restore_value: true
    initial_value: "false"

  # For the auto-off automation
  - id: glbl_timeout_enabled
    type: bool
    restore_value: true
    initial_value: "true"

  - id: glbl_timeout_length_ticks
    type: int
    restore_value: true
    # 5 min * 60 seconds = 300
    initial_value: "300"

  # We ALSO need to keep track of the number of 'ticks'
  # add _prefix to indicate 'internal'
  - id: _glbl_timeout_ticks
    type: int
    restore_value: false
    initial_value: "0"

  # entity id for Hoem Assistant timer 
  - id: glbl_timer_entity_id
    type: std::string
    restore_value: true

script:
    # Start meaning starting the timer actions
  - id: start_timer
    mode: single
    then:
      - script.execute: _timer_tick
      - homeassistant.action: 
          action: timer.start
          data_template:
            duration: !lambda return id(glbl_timeout_length_ticks);
            entity_id: !lambda return id(glbl_timer_entity_id);
      - homeassistant.event:
          event: esphome.${event}
          data:
            entity_id: esphome.${name}
            event_type: "timer"
            event_value: "start"
          data_template:
            timer_id: !lambda return id(glbl_timer_entity_id);

      - lambda: |-
          auto TAG = "script.start_timer";

          ESP_LOGD(TAG, "start_timer now started and glbl_timer_entity_id is %s", id(glbl_timer_entity_id).c_str());  

    # End meaning the natural conclusion of the timer. Do whatever we're supposed to do when the timer fires off
  - id: on_timer_end
    mode: single
    then:
      - light.turn_off: light_1
      - logger.log: "on_timer_end: output should be off!"
    # Stop meaning the pre-mature ending of the timer
  - id: on_timer_stop
    # Do not start a new run. Issue a warning.
    mode: single
    then:
      - homeassistant.action: 
          action: timer.cancel
          data_template:
            entity_id: !lambda return id(glbl_timer_entity_id);
        # For now, just clean up the globals and stop the ticking.
        # This hook could be used to do so much more, though.
        ##
      - lambda: |-
          auto TAG = "script.on_timer_stop";

          id(_timer_tick).stop();
          id(_glbl_timeout_ticks) = 0;
          ESP_LOGD(TAG, "_timer_tick now stopped and _glbl_timeout_ticks is %d", id(_glbl_timeout_ticks));          

  - id: on_timer_restart
    # Do not start a new run. Issue a warning.
    mode: single
    then:
        # For now, just clean up the globals and stop the ticking.
        # This hook could be used to do so much more, though.
        ##
      - homeassistant.action: 
          action: timer.start
          data_template:
            duration: !lambda return id(glbl_timeout_length_ticks);
            entity_id: !lambda return id(glbl_timer_entity_id);
      - lambda: |-
          auto TAG = "script.on_timer_restart";

          id(_glbl_timeout_ticks) = 0;
          if (not id(_timer_tick).is_running()) {
            id(_timer_tick).execute();
            ESP_LOGI(TAG, "_timer_tick script was not running!");
          }
          ESP_LOGD(TAG, "_timer_tick restarted and _glbl_timeout_ticks is %d", id(_glbl_timeout_ticks));    

  - id: _timer_tick
    # Start a new run after previous runs completes. This will happen until timer.stop() is called on us
    ##
    mode: queued
    then:
      # A single 'tick' is 1 second long
      - delay: 1s
      - lambda: |-
          auto TAG = "lambda._timer_tick";

          // First, update the number of ticks
          id(_glbl_timeout_ticks) += 1;

          // Then check if we have timed out
          if (id(_glbl_timeout_ticks) >= id(glbl_timeout_length_ticks) ) {

            // If we have timed out, run the script to handle the timer expiration
            // It's cleaner to call out to a script rather than put all the "what no?" code in here!
            id(on_timer_end).execute();

            ESP_LOGD(TAG, "_glbl_timeout_ticks is >= glbl_timeout_length_ticks  %d >= %d ", id(_glbl_timeout_ticks), id(glbl_timeout_length_ticks) );

            // And then re-set the internal counter
            id(_glbl_timeout_ticks) = 0;

            // And finally, stop the ticking timer
            id(_timer_tick).stop();
            ESP_LOGD(TAG, "_timer_tick now stopped!");

          } else {

            ESP_LOGD(TAG, "_glbl_timeout_ticks is < glbl_timeout_length_ticks  %d < %d ", id(_glbl_timeout_ticks), id(glbl_timeout_length_ticks) );

            // make sure we run again.. unless we're not supposed to
            if( id(glbl_timeout_enabled) ) {
              id(_timer_tick).execute();
            }

          }          

# Create a toggle in HA that allows us to arm/disarm the button <-> relay glue
# See: https://esphome.io/components/switch/template.html
switch:
  - id: sw_relay_mode
    name: "Detach Relay"
    platform: template
    device_class: "switch"
    restore_mode: RESTORE_DEFAULT_ON
    entity_category: "config"
    lambda: |-
      if (id(glbl_detach_relay)) {
        return true;
      } else {
        return false;
      }      

    turn_on_action:
      - globals.set:
          id: glbl_detach_relay
          value: "true"

    turn_off_action:
      - globals.set:
          id: glbl_detach_relay
          value: "false"

    # UI toggle for the arm/disarm of the auto-off/timeout functionality
  - id: sw_timeout_enabled
    name: "Timeout Automation"
    platform: template
    device_class: "switch"
    entity_category: "config"
    restore_mode: RESTORE_DEFAULT_ON

    lambda: |-
      if (id(glbl_timeout_enabled)) {
        return true;
      } else {
        return false;
      }      

    turn_on_action:
      then:
        # Update the global to store the new state
        # If the light is already on, also start the timer
        - lambda: |-
            id(glbl_timeout_enabled) = true;

            auto TAG = "template.Timeout Automation.turn_on_action";
            if ( id(light_1).current_values.is_on() ) {
              id(start_timer).execute();
            } else {
              ESP_LOGD(TAG, "Timeout Automation enabled, light NOT on. Nothing to do!");
            }            

    turn_off_action:
      then:
        # Update the global and stop the ticking timer if needed
        - lambda: |-
            // Set the global to OFF, it will be checked next time the _tick fires if the on_timer_stop doesn't
            // kill the ticking
            id(glbl_timeout_enabled) = false;
            id(on_timer_stop).execute();            

# Give the user a graphical control over the timeout
# See: https://esphome.io/components/number/template.html
number:
  - id: timeout_length
    platform: template
    name: "Timeout"
    entity_category: "config"
    # TODO: maybe it's a better UX to do this in minutes and do the conversion in esphome
    unit_of_measurement: seconds
    mode: box
    min_value: 30
    max_value: 21600
    step: 30

    lambda: |-
      return (int) id(glbl_timeout_length_ticks);      

    set_action:
      then:
        - globals.set:
            id: glbl_timeout_length_ticks
            value: !lambda |-
              // TODO: we're relying on HA to pass an integer; perhaps we should do atoi() and catch any exceptions
              return (int) x;

text:
  - id: timer_entity_id
    platform: template
    name: "Light Timer"
#    initial_value: id(glbl_timer_entity_id)
    optimistic: true
    min_length: 0
    max_length: 100
    mode: text
    restore_value: true
    on_value:
      then:
        - globals.set:
            id: glbl_timer_entity_id
            value: !lambda 'return x;' # x represents the value of the text_template

      

binary_sensor:
  - id: button_1
    name: Button
    platform: gpio
    entity_category: "diagnostic"
    pin:
      number: GPIO0
      inverted: true
    on_click:
      min_length: 50ms
      max_length: 350ms
      then:
        - if:
            # If the input -> output functionality is enabled
            condition:
              lambda: |-
                if (id(glbl_detach_relay)) {
                  return true;
                } else {
                  return false;
                }  
            then:
              - logger.log:
                  level: DEBUG
                  format: "Button1 pressed but relays unbound"

            else:
              - light.toggle:
                  id: light_1
              - logger.log:
                  level: DEBUG
                  format: "Button1 pressed, toggle light called."
  
  - id: switch_2
    platform: gpio
    name: Door
    pin: 
      number: GPIO01
      inverted: false
      mode:
        input: true
        pullup: true
    device_class: door
#    entity_category: config
    
#if door opens restart the timer
    on_press:
      then:
        - if:
            condition:
              light.is_on: light_1
            then:
              - script.execute: on_timer_restart
              - logger.log:
                  level: DEBUG
                  format: "Door opened while light on... restart timer."

  - id: switch_3
    platform: gpio
#    entity_category: "diagnostic"
    pin: GPIO03
    name: Motion
    device_class: motion
    on_release:
      then:
        - if:
            condition:
              binary_sensor.is_on: switch_2
            then:
              - if:
                  # If the button is bound to relay
                  condition:
                    lambda: |-
                      if (id(glbl_detach_relay)) {
                        return false;
                      } else {
                        return true;
                      }  
                  then:
                    - script.execute: on_timer_restart

#              - logger.log: "Motion ended Start turn off timer"            
#              - delay: 5min
#              - light.turn_off: light_1
#              - logger.log: "Turn Off Timmer ended.. turn off light"   
#            else:
#              - logger.log: "switch_2 is off"

    on_press:
      then:
        - if:
            #if the door open?
            condition:
              binary_sensor.is_on: switch_2
            then:
              - logger.log: "Door is Open"   
              - if:
                  # If the input -> output functionality is enabled
                  condition:
                    lambda: |-
                      if (id(glbl_detach_relay)) {
                        return false;
                      } else {
                        return true;
                      }  
                  then:
                    - script.execute: on_timer_restart
                    - light.turn_on:
                        id: light_1
                  else:
                    - logger.log:
                        level: DEBUG
                        format: "Button1 pressed but relays unbound"
            else:
              - script.execute: on_timer_stop
              - logger.log:
                  level: DEBUG
                  format: "Motion detected, door is closed... stop timer."  
#        - light.turn_on: light_1             


# output:
#  - platform: gpio
#    pin: GPIO12
#    id: relay_1
output:
  # See" https://esphome.io/components/output/ledc.html
  - id: relay_1
    platform: gpio
    pin:
      number: 12

  - id: gpio_13
    platform: gpio
    pin: 
      number: 13

light:
  # See: https://esphome.io/components/light/binary.html
  - id: light_1
    name: Light
    platform: binary
    output: relay_1

    # Resume last state on boot if possible. Else, off
    restore_mode: RESTORE_DEFAULT_OFF

    # Wire in the count down timer automation if enabled
    on_turn_on:
      then:
        - if:
            condition:
              # If the countdown timer is enabled
              lambda: 'return id(glbl_timeout_enabled);'
            then:
              # The light is already on, start counting the seconds.
              # When timer ends, light will be turned off
              - script.execute: start_timer
            else:
              - logger.log:
                  level: DEBUG
                  format: "Light1 turned on, countdown timer not enabled"

    # This can be called by the natural end of the timer OR manually through any other source.
    # Regardless of the source, we just need to stop the ticking if it's running.
    on_turn_off:
      then:
        - if:
            condition:
              lambda: 'return id(_timer_tick).is_running();'
            then:
              - script.execute: on_timer_stop

            else:
              - logger.log:
                  level: DEBUG
                  format: "Light1 turned off, countdown timer not enabled"

  - id: relay_status_leds
    name: "Status Lights"
    platform: status_led
    output: gpio_13
    # Classify this as a "config" entity rather than a primary entity
    entity_category: config
    restore_mode: RESTORE_DEFAULT_ON

    # The LEDs technically do support some effects! Although there's really only one 'built-in' effect that looks
    #   any good on the tiny LEDs / switch.
    
    #effects:
    #  - pulse:
    #      transition_length: 1s
    #      update_interval: 1s

#  - platform: binary
#    name: "Light"
#    output: relay_1
#    id: light_1

#  - platform: status_led
#    name: "Switch state"
#    pin: 
#      number: GPIO13
#      inverted: true
#    internal: true

# binary_sensor:
#   - platform: gpio
#     pin: 
#       number: GPIO0
#       inverted: true # Example for button 1
#     name: "Button 1"
#     id: button_1
#     internal: true
#     on_press:
#       - light.toggle: light_1
