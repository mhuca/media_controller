- id: AutoGen2-1154-4917-a55d-f7997f73481a
  alias: zDelete - Timer Powder Room Light Timer changes Event
  description: Native Timer Powder Room Light Timer changes Event
  triggers:
  - trigger: event
    event_type: timer.finished
    event_data:
      entity_id: timer.powder_room_light_timer
  conditions: []
  actions:
  - sequence:
    - action: light.turn_off
      metadata: {}
      data:
        transition: 60
      target:
        area_id:
        - powder_room
    - action: input_boolean.turn_off
      metadata: {}
      data: {}
      target:
        entity_id:
        - input_boolean.powder_room_light_override

# 
# {% set players = label_entities('home_music')
#                   | select('match', 'media_player.')
#                   | list %}
                  
# {%- set playing_local = states.media_player 
#   | selectattr('state', 'eq', 'playing')
#   | selectattr('attributes.source', 'defined')
#   | selectattr('attributes.source', 'ne', 'Optical')
#   | map(attribute='entity_id')
#   | select('in', players )
#   | list 
# %}

# {%- set playing_opt = players | difference(states.media_player 
#   | selectattr('state', 'eq', 'playing')
#   | selectattr('attributes.source', 'defined')
#   | selectattr('attributes.source', 'eq', 'Optical')
#   | map(attribute='entity_id')
#   | select('in', players )
#   | list ) | list
# %}
# {{ playing_local[0] if playing_local  | length > 0 else playing_opt[0] }}



# 